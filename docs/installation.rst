.. highlight:: shell

============
Installation
============


Stable release
--------------

To install libvirt_provider, run this command in your terminal:

.. code-block:: console

    $ pip install libvirt_provider

This is the preferred method to install libvirt_provider, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for libvirt_provider can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/dguitarbite/python-libvirt_provider

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/dguitarbite/python-libvirt_provider/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/dguitarbite/python-libvirt_provider
.. _tarball: https://github.com/dguitarbite/python-libvirt_provider/tarball/master
